// Create an array of favorite singers
let favSingers = ['Dionela', 'Eminem', 'IU'];
// Log the first singer
console.log(favSingers[0]);

// Create an array of favorite numbers
let favNumbers = [7, 11, 21, 42];

// Create an array with mixed data types
let mixedArr = ["string", ["otherarray"], 123, true];

// Access and log each item in mixedArr
for (let i = 0; i < mixedArr.length; i++) {
    console.log(mixedArr[i]);
}
